import {Box} from '@mui/material';
import {ReactElement} from 'react';

const SortBlock = (): ReactElement => {
  return <Box className="flex justify-start">[Sort Block]</Box>;
};

export default SortBlock;
