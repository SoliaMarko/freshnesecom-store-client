import {ReactElement} from 'react';
import {Box} from '@mui/material';

const ProductDetails = (): ReactElement => {
  return <Box>[Product Details Page]</Box>;
};

export default ProductDetails;
